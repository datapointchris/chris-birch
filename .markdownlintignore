# Blog posts are published content validated by Astro content collections,
# not repo docs — doc-oriented markdown rules (single-h1, first-line-heading)
# conflict with frontmatter + post structure.
src/content/

# Regenerated by semantic-release on every release, so any normalization
# here is undone on the next one — and reappears as a conflict when a
# local commit lands on top of the release commit.
CHANGELOG.md
